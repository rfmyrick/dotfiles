#
# My highly opinionated tmux configuration.
#
# Ideas for this configuration were taken from many content contributors including:
#   omerxx
#   dreamsofcode
# 
# This plugin depends on the tpm which can be installed using:
#   git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm/
#
# Notes:
#   - The prefix key is mapped to C-a
#
#   - Panes can be split using Prefix-- and Prefix-|
#   - Pressing C-a, C-a, will cycle through the panes making navigation easier.
#   - Panes can be navigated using Prefix-h, Prefix-j, Prefix-km and Prefix-l
#   - A pane can be closed (killed) with Prefix-r
#
#
#
#
#   - The ~/.config/tmux/tmux.conf can be reloaded using Prefix-r
#


source-file ~/.config/tmux/tmux.reset.conf
set-option -g default-terminal 'screen-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

set -g prefix ^A
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g status-position top
set -g default-terminal "${TERM}"
set -g mode-keys vi
set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'

set -g @fzf-url-fzf-options '-p 60%,30% --prompt="   " --border-label=" Open URL "'
set -g @fzf-url-history-limit '2000'

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'wfxr/tmux-fzf-url'
set -g @plugin 'omerxx/catppuccin-tmux' # My fork that holds the meetings script bc I'm lazy af
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'omerxx/tmux-floax'
set -g @floax-width '80%'
set -g @floax-height '80%'
set -g @floax-border-color 'magenta'
set -g @floax-text-color 'blue'
set -g @floax-bind 'p'
set -g @floax-change-path 'true'
set -g @sessionx-bind-zo-new-window 'ctrl-y'
set -g @sessionx-auto-accept 'off'
set -g @sessionx-custom-paths '/Users/rfmyrick/dotfiles'
set -g @sessionx-bind 'o'
set -g @sessionx-x-path '~/dotfiles'
set -g @sessionx-window-height '85%'
set -g @sessionx-window-width '75%'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-custom-paths-subdirectories 'false'
set -g @sessionx-filter-current 'false'
set -g @continuum-restore 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W#{?window_zoomed_flag,(),}"
set -g @catppuccin_status_modules_right "directory date_time"
set -g @catppuccin_status_modules_left "session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_directory_text "#{b:pane_current_path}"
set -g @catppuccin_meetings_text "#($HOME/.config/tmux/scripts/cal.sh)"
set -g @catppuccin_date_time_text "%H:%M"

run '~/.config/tmux/plugins/tpm/tpm'

#-----
#
set -g mouse on

#unbind C-b  # change the Prefix key from C-b to C-a
#set -g prefix C-a

unbind ^A  # multiple C-a toggles through the windows
bind ^A select-pane -t :.+

unbind %  # change split vertical to | and set to the directoy of the current pane
bind | split-window -h -c "#{pane_current_path}"

unbind '"'  # change split horizontal to - and set to the directory of the current pane 
bind - split-window -v -c "#{pane_current_path}"

#unbind r  # set Prefix-r to reload the tmux configuration
#bind r source-file ~/.config/tmux/tmux.conf

## Vim style keys for pane selection
#bind h select-pane -L
#bind j select-pane -D 
#bind k select-pane -U
#bind l select-pane -R

## Use Vim style keys to resize panes
##bind h resize-pane -L 5
##bind j resize-pane -D 5
##bind k resize-pane -U 5
##bind l resize-pane -R 5

## Start windows and panes at 1, not 0
#set -g base-index 1
#set -g pane-base-index 1
#set-window-option -g pane-base-index 1
#set-option -g renumber-windows on

## Use Alt-arrow keys without prefix key to switch panes
#bind -n M-Left select-pane -L
#bind -n M-Right select-pane -R
#bind -n M-Up select-pane -U
#bind -n M-Down select-pane -D

## Shift arrow to switch windows
#bind -n S-Left  previous-window
#bind -n S-Right next-window

## Shift Alt vim keys to switch windows
#bind -n M-H previous-window
#bind -n M-L next-window

#bind -r m resize-pane -Z # toggle zooming in and out of the current pane

#set -sg escape-time 10  # reduce the delay for processing the ESC key from 500ms to 10ms making vim/nvim feel more responsive

#set -g @plugin 'tmux-plugins/tpm'
#set -g @plugin 'tmux-plugins/tmux-sensible'
#set -g @plugin 'tmux-plugins/tmux-yank'
#set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
#set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes
#set -g @plugin 'christoomey/vim-tmux-navigator'
#set -g @plugin 'fcsonline/tmux-fzf'
#set -g @plugin 'wfxr/tmux-fzf-url'
#set -g @plugin 'omerxx/tmux-session-manager'
#set -g @plubin 'omerxx/catppuccin-tmux'
# set -g @plugin 'catppuccin/tmux'

#set -g @resurrect-strategy-nvim 'session'

#set -g @catppuccin_flavour 'latte'
#set -g @catppuccin_window_tabs_enabled on # or off to disable window_tabs
#set -g @catppuccin_left_separator "█"
#set -g @catppuccin_right_separator "█"
#set -g @catppuccin_date_time "%Y-%m-%d %H:%M"
#set -g @catppuccin_user "on"
#set -g @catppuccin_host "off"
#set -g @resurrect-capture-pane-contents 'on'
#set -g @continuum-restore 'on'

## set vi-mode
#set-window-option -g mode-keys vi

## keybindings
#bind-key -T copy-mode-vi v send-keys -X begin-selection
#bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
#bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
#unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

## Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
#run '~/.tmux/plugins/tpm/tpm'
